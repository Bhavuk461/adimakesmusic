node_modules
dist
*.local
.DS_Store
Thumbs.db

# local tooling
.claude/

# raw source assets — the site serves the copies in /public
# (scoped to the raw files so /audio and /pics inside a deploy tree stay publishable)
/audio/WhatsApp*
/pics/WhatsApp*
/nul
